name: Ping Services

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping-services:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: curl -s https://analyticswithavery.com > /dev/null
